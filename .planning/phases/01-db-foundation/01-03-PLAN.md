---
phase: 01-db-foundation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase-schema.sql
autonomous: true
requirements:
  - "Enables GROC-02 (two-section layout), GROC-03 (add inventory items), GROC-04 (increment/decrement), GROC-05 (auto-restock), GROC-06 (restock from shopping list)"
must_haves:
  truths:
    - "supabase-schema.sql contains ALTER TABLE public.groceries ADD COLUMN IF NOT EXISTS is_inventory"
    - "supabase-schema.sql contains ALTER TABLE public.groceries ADD COLUMN IF NOT EXISTS stock_count"
  artifacts:
    - "supabase-schema.sql — updated with inventory columns migration block for groceries table"
  key_links: []
---

<objective>
Add two columns to the existing `groceries` table to support inventory management: an `is_inventory` boolean flag and a `stock_count` integer for numeric quantity tracking.

Purpose: Phase 2 (Groceries — Icons & Inventory) needs to distinguish shopping list items from inventory items (is_inventory) and track stock quantities as integers for increment/decrement operations (stock_count). The existing `quantity` column is text (used for display like "2 lbs") and cannot be used for arithmetic — a separate integer column is required.

Output: supabase-schema.sql updated with two ALTER TABLE statements for public.groceries:
- is_inventory boolean NOT NULL DEFAULT false
- stock_count integer NOT NULL DEFAULT 1
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Relevant source files:
@supabase-schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append inventory columns migration to supabase-schema.sql</name>
  <files>supabase-schema.sql</files>
  <read_first>supabase-schema.sql</read_first>
  <action>
Read supabase-schema.sql. Append a clearly-labeled Phase 1 migration block for the groceries table. Add it AFTER any existing content (append to end). Use IF NOT EXISTS guards.

Append exactly this block:

```sql
-- ──────────────────────────────────────────────────────────────
-- Phase 1: DB Foundation — groceries table inventory columns
-- ──────────────────────────────────────────────────────────────
alter table public.groceries add column if not exists is_inventory boolean not null default false;
alter table public.groceries add column if not exists stock_count integer not null default 1;
```

Column rationale:
- is_inventory: boolean NOT NULL default false — flag separating shopping list items from pantry/inventory items. Existing rows default to false (all existing items are shopping list items).
- stock_count: integer NOT NULL default 1 — numeric stock level for inventory items, used for increment/decrement in Phase 2. Default 1 is a sensible starting point. The existing text `quantity` column stays for shopping list display quantities (e.g. "2 lbs", "1 dozen").

Do NOT modify the existing `quantity text` column — it is used by the existing Groceries UI for shopping list item display and must remain text.
  </action>
  <verify>grep "is_inventory\|stock_count" supabase-schema.sql</verify>
  <acceptance_criteria>
    - "supabase-schema.sql contains 'add column if not exists is_inventory boolean not null default false'"
    - "supabase-schema.sql contains 'add column if not exists stock_count integer not null default 1'"
    - "Both ALTER TABLE statements target 'public.groceries'"
    - "Existing 'quantity text' column is unchanged (not modified or dropped)"
  </acceptance_criteria>
  <done>grep output shows 2 matches: is_inventory and stock_count on public.groceries</done>
</task>

</tasks>

<verification>
1. grep "is_inventory\|stock_count" supabase-schema.sql — expect 2 lines, both ALTER TABLE public.groceries
2. grep "quantity" supabase-schema.sql — existing 'quantity text' line still present in CREATE TABLE groceries block
3. No existing schema lines were modified
</verification>

<success_criteria>
1. supabase-schema.sql has 2 new ALTER TABLE public.groceries statements (is_inventory, stock_count)
2. is_inventory is boolean NOT NULL DEFAULT false
3. stock_count is integer NOT NULL DEFAULT 1
4. Both use ADD COLUMN IF NOT EXISTS (safe to re-run)
5. Existing quantity text column is untouched
</success_criteria>
